---
title: 微调数据准备
date: 2026-08-09
abstract: 微调数据准备的一些相关知识
tags:
- Ai知识概念
- 微调
---

# 微调数据准备

在微调大语言模型时，**数据是决定模型性能与适用性的关键因素**。模型表现很大程度上取决于训练数据的质量，高质量数据是微调效果的基石。根据数据的来源与可访问性，本章将数据准备分为三大板块展开：**数据来源、数据集格式、Chat Template 与数据质量控制**。


## 1. 数据来源

### 1.1 公共数据源

公共数据集易于获取，是微调项目起步阶段的绝佳选择。常用的数据共享平台有：

**Hugging Face Hub**

Hugging Face Hub 是不可或缺的 AI 资源平台，托管了数千个数据集。用户可通过 `datasets` 库轻松访问，并根据任务类型（如文本生成、文本摘要）、语言及数据许可证进行筛选，快速定位符合需求的数据。

**ModelScope**

ModelScope 是阿里巴巴旗下的模型开放平台，提供一系列高质量的开源模型与数据集。数据集覆盖自然语言处理、计算机视觉和多模态任务等多个领域，尤其对中文场景支持良好。

### 1.2 私有数据源

私有数据源是指企业或组织内部拥有的数据，可能受版权保护或隐私限制，仅限内部使用或特定授权范围内使用。常见的私有数据源包括：

| 类型 | 说明 | 典型用途 |
|------|------|---------|
| **企业内部文档** | 历史文件、报告、邮件、会议记录等 | 使模型理解公司业务流程和专业知识 |
| **客户反馈数据** | 客户评论、投诉、建议等 | 改进产品和服务质量 |
| **专有数据库** | 行业特有数据，如电子病历、金融交易记录等 | 构建行业专用模型 |

**私有数据处理要点**：

私有数据通常并非专为模型微调而准备，因此在使用前必须经过**清洗、结构化和答案构造（目标输出撰写）** 等预处理步骤。其中，答案构造是私有数据处理中最关键且最耗费人力的环节——需要根据业务场景为每条输入精心撰写高质量的期望输出，而非传统意义上的“贴标签”。企业可根据实际需求，由内部专业团队或通过外部众包平台对原始数据进行系统化整理。

此外，还可借助自动化工具提升效率，如使用 **Easy Dataset** 等开源方案。通过人机协同的工作模式，企业能够更高效地将私有知识转化为高质量微调数据，有力支撑模型在特定业务场景中的性能提升。


## 2. 数据集格式

在 SFT 中，数据集的构建格式至关重要。常见格式可分为两类：**指令式**和**对话式**。

### 2.1 指令式格式（Instruction Format）

指令式数据集用于训练模型执行明确的**单轮任务**，如翻译、摘要或问答。其典型格式源自斯坦福大学 **Alpaca** 项目，结构简洁、易于使用。

**结构组成**：

每条样本包含三个字段：

| 字段 | 说明 |
|------|------|
| **instruction** | 描述模型需要执行的任务指令 |
| **input** | 任务所需的上下文或附加信息（可选，可为空字符串） |
| **output** | 模型应生成的正确回答 |

**示例**：

```json
{
  "instruction": "将以下英文翻译成中文",
  "input": "Large language models are transforming AI.",
  "output": "大语言模型正在改变人工智能。"
}
```

**模板化应用**：

训练时，上述字段会通过提示模板组合成结构统一的输入字符串。当 `input` 不为空时：

```
### 指令：
将以下英文翻译成中文

### 输入：
Large language models are transforming AI.

### 回复：
大语言模型正在改变人工智能。
```

当 `input` 为空时，模板中通常省略“输入”部分，仅保留指令和回复：

```
### 指令：
解释一下什么是机器学习。

### 回复：
机器学习是人工智能的一个分支，它允许计算机系统通过数据和经验自动改进其性能。
```

### 2.2 对话式格式（Dialogue Format）

对话式数据集用于训练模型进行**多轮对话**，如聊天机器人、虚拟助手等。这类数据以消息序列的形式组织，强调发言者角色与对话流程。目前广泛采用的格式主要有两种。

#### 2.2.1 ShareGPT 格式

ShareGPT 格式源于用户在社区中分享的与 ChatGPT 的真实对话记录，常用于保存多轮对话数据集。

**结构组成**：

每条样本由一个 `conversations` 列表构成，列表中的每个元素代表一次发言，包含两个关键字段：

| 字段 | 说明 |
|------|------|
| **from** | 发言者角色，通常取值为 `"human"`（用户）或 `"gpt"`（模型助手） |
| **value** | 该轮对话的具体文本内容 |

**示例**：

```json
{
  "conversations": [
    {
      "from": "human",
      "value": "你能解释一下什么是机器学习吗？"
    },
    {
      "from": "gpt",
      "value": "机器学习是人工智能的一个分支，它允许计算机系统通过数据和经验自动改进其性能。"
    },
    {
      "from": "human",
      "value": "它主要有哪些类型？"
    },
    {
      "from": "gpt",
      "value": "主要类型包括监督学习、无监督学习、半监督学习和强化学习等。"
    }
  ]
}
```

#### 2.2.2 OpenAI 格式

OpenAI 格式是 OpenAI 官方在其 API 微调功能中推广的通用消息列表格式，兼具灵活性与表达力，可同时支持单轮和多轮对话。

**结构组成**：

每条样本包含一个 `messages` 字段，其中是一个有序列表，每条消息包含以下字段：

| 字段 | 说明 |
|------|------|
| **role** | 发言者角色，常见值有 `system`（系统设定）、`user`（用户）和 `assistant`（模型助手） |
| **content** | 消息的具体文本内容 |

**示例**：

```json
{
  "messages": [
    {
      "role": "system",
      "content": "你是一名专业的营养师。你的回答应基于科学知识，提供务实、健康的建议，并避免提供任何医疗诊断。"
    },
    {
      "role": "user",
      "content": "我最近感觉下午很疲惫，有什么健康的零食推荐吗？"
    },
    {
      "role": "assistant",
      "content": "下午感到疲惫是常见的现象，选择正确的零食可以帮助稳定能量水平。以下是一些推荐：\n1. **一把坚果和干果**：提供健康的脂肪、蛋白质和天然糖分，能快速补充能量。\n2. **希腊酸奶**：富含蛋白质，能提供持久的饱腹感。\n3. **一个苹果或一根香蕉**：富含维生素和膳食纤维，是方便的天然能量来源。\n请记得结合充足的水分摄入，因为脱水也会导致疲劳。"
    }
  ]
}
```

### 2.3 两种对话式格式的对比与转换

#### 2.3.1 格式对比

| 维度 | ShareGPT 格式 | OpenAI 格式 |
|------|--------------|------------|
| 角色字段名 | `from` | `role` |
| 用户角色值 | `human` | `user` |
| 助手的角色值 | `gpt` | `assistant` |
| 系统提示支持 | ❌ 不支持 | ✅ 支持（通过 `system` 角色） |
| 来源 | 社区共享对话 | OpenAI API 微调标准 |
| 适用场景 | 通用多轮对话数据存储与交换 | 与 OpenAI API 兼容的训练流程 |

#### 2.3.2 字段映射关系

实际数据处理中，常需在两种格式间转换，字段映射关系如下：

| ShareGPT | OpenAI | 说明 |
|----------|--------|------|
| `from: "human"` | `role: "user"` | 用户提问 |
| `from: "gpt"` | `role: "assistant"` | 模型回答 |
| 不支持 | `role: "system"` | 系统提示（ShareGPT 无对应概念，需新建或丢弃） |

#### 2.3.3 多轮对话构造的特殊约束

在构造多轮对话数据时，需遵循以下规则：

| 约束 | 说明 | 错误示例 |
|------|------|---------|
| **角色交替** | human 和 gpt 必须交替出现，不能连续两条同一角色 | `[human, human, gpt]` ❌ |
| **首轮为 human** | 对话应以用户提问开始 | `[gpt, human, gpt]` ❌ |
| **末轮为 gpt** | 对话应以模型回答结束 | `[human, gpt, human]` ❌ |
| **system 可选** | system 可置于对话最前面，不计入交替规则 | `[system, human, gpt]` ✅ |

**System 提示的使用场景**：

- 设定模型角色（如“你是一名专业营养师”）
- 约束回答风格（如“回答应简洁，不超过50字”）
- 提供背景知识或行为准则（如“避免提供医疗诊断”）
- 指定输出格式（如“始终以 JSON 格式输出”）

### 2.4 数据处理两阶段流程

在实际训练中，数据格式的处理分为两个层次：

**阶段一：格式标准化** —— 将来源各异的原始数据统一转换为某种标准格式（如 ShareGPT 或 OpenAI 格式）。这一阶段解决的是数据来源多样性问题。

**阶段二：模板化** —— 通过 Chat Template 将标准格式转换为模型输入字符串。这一阶段解决的是模型输入格式适配问题。

两个阶段的关系如下：

```
原始数据（来源多样）
    ↓ 【阶段一：格式标准化】
标准化格式（ShareGPT / OpenAI 格式）
    ↓ 【阶段二：Chat Template 模板化】
模型输入字符串（含特殊 token 的文本）
```


## 3. Chat Template：从格式到训练输入

### 3.1 Chat Template 的作用

Chat Template 是将标准化格式的多轮对话转换为模型可接收的字符串格式的转换函数。不同模型使用不同的模板格式和特殊 token。

**常见模型的 Chat Template 示例**：

| 模型系列 | 模板格式示例 | 特殊 Token |
|---------|------------|-----------|
| **ChatML（Qwen/通义千问）** | `<|im_start|>user\n内容<|im_end|>\n<|im_start|>assistant\n内容<|im_end|>` | `<|im_start|>`、`<|im_end|>` |
| **Llama 2/3** | `[INST] 内容 [/INST] 回复 </s>` | `[INST]`、`[/INST]`、`</s>` |
| **DeepSeek** | `用户：内容\n\n助手：内容\n\n`（含 `think` 标签） | `okenizer.eos_token` |

以 ChatML 模板为例，原始对话数据：

```json
[
  {"content": "你好，你是谁？", "role": "user"},
  {"content": "我是一个智能助手", "role": "assistant"}
]
```

经 Chat Template 转换后：

```
<|im_start|>user
你好，你是谁？<|im_end|>
<|im_start|>assistant
我是一个智能助手<|im_end|>
```

> **注意**：图中 `<|im_start|>assistant` 后展示的 `<|think|></think>` 是 DeepSeek 系列模型特有的推理过程标记，用于引导模型在输出最终回答前进行推理思考。不同模型的 Chat Template 格式和特殊 token 各不相同，实际使用时需匹配对应模型的模板。

### 3.2 损失计算范围

在使用 Chat Template 将多轮对话转换为训练字符串后，模型训练时**仅对 assistant 角色（模型回答部分）的 tokens 计算损失**，不对 user 和 system 部分的 tokens 计算损失。

这一机制通过设置 `ignore_index = -100` 来实现：

```python
# 伪代码示意
labels = input_ids.copy()
labels[非 assistant 位置] = -100  # 仅 assistant 部分参与损失计算，其余被忽略
```

这样做的目的是确保模型只学习“如何正确回答”，而无需记忆或复述用户提问，从而提高训练效率和回答质量。

### 3.3 `add_generation_prompt` 参数

在推理/生成阶段，通常需要在对话末尾添加 `assistant` 开始标记，提示模型开始生成回答。各框架通过 `add_generation_prompt` 参数控制：

```python
# HuggingFace Transformers 示例
tokenizer.apply_chat_template(
    messages, 
    add_generation_prompt=True,  # 在末尾添加 assistant 开始标记
    tokenize=True,
    return_tensors="pt"
)
```

当 `add_generation_prompt=True` 时，模板输出会在对话末尾追加模型角色开始标记：

```
<|im_start|>user
你好，你是谁？<|im_end|>
<|im_start|>assistant
                                    ← 此处为模型开始生成的位置
```


## 4. 数据质量控制

高质量数据是微调效果的保障，在数据处理过程中应建立系统化的质量控制机制。

### 4.1 数据清洗维度

| 维度 | 说明 | 常用方法 |
|------|------|---------|
| **去重** | 完全重复的样本会导致模型过拟合，需剔除 | 文本哈希（MD5/SHA）、MinHash |
| **长度过滤** | 剔除过短（无意义）或过长（超出上下文窗口）的样本 | 按 token 长度阈值过滤 |
| **格式校验** | 确保每条数据格式正确，字段完整 | JSON Schema 校验、正则匹配 |
| **内容质量** | 剔除格式错误、无意义、包含有害内容的样本 | 人工抽检、规则过滤、模型评分 |
| **隐私脱敏** | 私有数据需去除敏感信息（姓名、电话、地址等），满足合规要求 | 正则脱敏、NER 识别替换 |

### 4.2 数据平衡

类别分布不均时，模型会偏向多数类，导致少数类样本表现差。常用处理方式：

- **过采样**：复制少数类样本或使用数据增强生成更多样本
- **欠采样**：从多数类中随机抽取部分样本，缩小数量差距
- **重加权**：在损失计算时为少数类赋予更高权重

对于对话式数据，“类别”通常指任务类型或场景类型，需确保训练数据覆盖各类业务场景。

### 4.3 数据规模参考

初学者常问“需要多少条数据”，以下为经验参考范围：

| 任务复杂度 | 推荐数据量 | 核心原则 |
|-----------|-----------|---------|
| **简单任务**（分类、格式输出、短文本转换） | 几百 ~ 几千条 | **格式规范比数量更重要** |
| **中等任务**（客服 FAQ、摘要生成） | 数千 ~ 2万条 | 覆盖主要场景和常见问题 |
| **复杂任务**（NL2SQL、推理、长文本生成） | 1万 ~ 10万条 | 保证数据多样性和覆盖度 |
| **知识注入**（行业领域知识） | 根据知识广度而定 | 重点在于知识覆盖度而非数量 |

> ⚠️ 核心原则：**数据质量 > 数据数量**。千条精心构造的高质量数据，往往优于万条未经筛选的低质量数据。在扩大数据量之前，应优先确保每条数据的质量达标。

### 4.4 公共数据与私有数据的混合策略

对于行业微调项目，通常需要将公共通用数据与私有领域数据混合使用：

| 策略 | 说明 | 适用场景 |
|------|------|---------|
| **直接混合** | 将两类数据按比例混合后一起训练 | 数据量充足、两类数据格式一致 |
| **两阶段训练** | 先在通用数据上预热（1-2 epoch），再在领域数据上精调（1-2 epoch） | 私有数据量较少时，可保留通用能力 |
| **比例控制** | 控制私有数据在总数据中的占比 | 通用场景下避免领域偏置 |

**建议比例**：私有数据占比建议不低于 **10%~20%**，以确保模型充分学到领域特征；同时保留一定比例（建议不低于 10%）的通用数据，以缓解**灾难性遗忘**问题。

> **灾难性遗忘**：模型在专注于学习新领域知识时，可能会遗忘之前在预训练阶段学到的通用知识和能力。通过在训练数据中混合通用数据，可有效缓解这一问题。


## 本章核心要点

| 主题 | 核心要点 |
|------|---------|
| **数据来源** | 公共数据源（HuggingFace、ModelScope）适合起步探索；私有数据源（企业文档、客户反馈）需经清洗、结构化、答案构造等预处理，可借助自动化工具提升效率 |
| **指令式格式** | 适用单轮任务，由 instruction/input/output 三字段构成；input 可为空，模板中可省略输入部分 |
| **ShareGPT 格式** | 适用多轮对话，由 conversations 列表 + from/value 字段构成，角色为 human/gpt |
| **OpenAI 格式** | 适用多轮对话（含系统提示），由 messages 列表 + role/content 字段构成，角色为 system/user/assistant |
| **格式转换** | 原始数据需经“格式标准化”和“Chat Template 模板化”两阶段处理 |
| **Chat Template** | 不同模型对应不同模板（ChatML、Llama、DeepSeek 等），训练时仅 assistant 部分计算损失 |
| **数据质量控制** | 涵盖去重、长度过滤、格式校验、隐私脱敏、类别平衡等维度 |
| **数据规模** | 几百（简单）到数万条（复杂），核心原则是质量 > 数量 |
| **数据混合** | 私有数据建议占 10%-20%，混合通用数据可缓解灾难性遗忘 |

> 规范的数据准备流程——从合理选择数据源、系统化质量控制、按任务需求选择合适的格式，到正确应用 Chat Template——是成功开展 SFT 项目的基础保障。后续章节将进一步介绍数据预处理的具体方法、数据增强策略以及进阶的数据工程技巧。
